Proposed next improvements

1. Add PWA icons and a favicon set referenced by the manifest and `index.html`
2. Add optional service worker scaffold for offline caching
3. Add a minimal `npm run lint` setup for JS/CSS consistency
4. Add a lightweight layout component system (header/footer helpers)
5. Add optional environment-based config module (dev/prod overrides)
